apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: keycloak

resources:
  - namespace.yaml
  - ingress.yaml

# Note: Keycloak is installed via Helm chart (bitnami/keycloak).
# This base keeps reusable namespace + ingress manifests.

