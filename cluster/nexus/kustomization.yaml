apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nexus

resources:
  - namespace.yaml
  - ingress.yaml

# Note: Nexus Repository Manager is installed via Helm chart (sonatype/nexus-repository-manager).
# Client repos should create an ArgoCD Application with a Helm source and reference `values.yaml` from this module.

