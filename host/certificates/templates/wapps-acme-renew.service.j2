[Unit]
Description={{ platform_slug }} ACME certificate renewal (acme.sh)
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
EnvironmentFile=-{{ platform_config_dir }}/acme.env
ExecStart=/root/.acme.sh/acme.sh --cron --home /root/.acme.sh

